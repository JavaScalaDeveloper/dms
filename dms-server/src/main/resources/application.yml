server:
  port: 9000

spring:
  application:
    name: dms-server
  datasource:
    url: jdbc:mysql://book-i7-4mq:3306/dms?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jmx:
    enabled: false

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

# 禁用REST风格URL匹配
spring.mvc.hiddenmethod.filter.enabled: false